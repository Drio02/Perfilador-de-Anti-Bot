pytest==8.3.3            # tests
pytest-asyncio==0.24.0   # si los sondeos pasan a async en la Fase 3
respx==0.21.1            # mockea httpx: tests sin tocar internet
ruff==0.7.4              # linter + formatter, sustituye a flake8/black/isort
mypy==1.13.0             # comprobación estática de tipos
